# แนวปฏิบัติสำหรับโครงการ Smart IV Alert

## การออกเวอร์ชันและแบรนช์

**ทุกครั้งที่สร้างเวอร์ชันใหม่หรือปรับปรุงเวอร์ชันของเฟิร์มแวร์ ต้องสร้างแบรนช์กำกับไว้เสมอ**
เพื่อให้ย้อนกลับไปดูสภาพของรีโพ ณ เวอร์ชันนั้นได้ในภายหลัง

### คอลเลกชัน

แบ่งตาม **ชนิดของหน้าจอที่ใช้** เพราะนั่นคือสิ่งเดียวที่ทำให้สายเวอร์ชันต่างกัน

| คอลเลกชัน | อุปกรณ์ | จอ | โฟลเดอร์เฟิร์มแวร์ |
|---|---|---|---|
| `station` | เครื่องประจำเตียง | TFT ในตัว | `ESP32-S3-Station-V_*` |
| `oled` | Host กลาง | SH1106 128×64 ขาว-น้ำเงิน | `ESP32-S3-Host-OLED-V_*` และ `ESP32-S3-Host-V_4_7_0` (รุ่นแรก ยังไม่ได้ใส่คำว่า OLED ในชื่อ) |
| `tft28` | Host กลาง | ST7789V 2.8" 320×240 แนวนอน | `ESP32-S3-Host-TFT-V_*` |
| `touch24` | Host กลาง | ILI9341 + XPT2046 2.4" 240×320 แนวตั้ง สัมผัสได้ | `ESP32-S3-Host-Touch-V_*` |

**เวอร์ชันใหม่ต้องลงในคอลเลกชันที่ตรงกับชนิดจอเสมอ**
ถ้าทำเวอร์ชันที่ใช้จอชนิดใหม่ที่ยังไม่มีในตาราง ให้เพิ่มคอลเลกชันใหม่ อย่ายัดลงของเดิม

### รูปแบบชื่อแบรนช์

```
release/<คอลเลกชัน>/v<เลขเวอร์ชัน>
```

| โฟลเดอร์เฟิร์มแวร์ | ชื่อแบรนช์ |
|---|---|
| `firmware/ESP32-S3-Station-V_7_7_3/` | `release/station/v7.7.3` |
| `firmware/ESP32-S3-Host-V_4_7_0/` | `release/oled/v4.7.0` |
| `firmware/ESP32-S3-Host-OLED-V_4_7_5/` | `release/oled/v4.7.5` |
| `firmware/ESP32-S3-Host-TFT-V_4_8_0/` | `release/tft28/v4.8.0` |
| `firmware/ESP32-S3-Host-Touch-V_4_9_2/` | `release/touch24/v4.9.2` |

ใช้ `/` คั่นคอลเลกชัน เพราะ GitHub จะจัดเป็นโฟลเดอร์ให้อัตโนมัติในหน้า Branches
กดพับ-กางดูทีละคอลเลกชันได้

### แบรนช์ต้องชี้ไปที่ commit ใด

ชี้ไปที่ **commit ล่าสุดที่แตะโฟลเดอร์ของเวอร์ชันนั้น** ไม่ใช่ปลายแบรนช์งาน
เพราะต้องการภาพของรีโพ ณ ตอนที่เวอร์ชันนั้นเสร็จ ไม่ใช่ภาพปัจจุบัน

### แบรนช์ต้องมีเฟิร์มแวร์กี่ชุด

**ชุดเดียว** คือเวอร์ชันที่แบรนช์นั้นตั้งชื่อตาม ไม่มีเวอร์ชันอื่นเลย
ไม่ว่าจะสายเดียวกันหรือสายอื่น เพราะเปิดแบรนช์มาแล้วต้องเห็นทันทีว่าตัวไหนคือตัวจริง

```
release/station/v7.7.3
└── firmware/
    ├── ESP32-S3-Station-V_7_7_3/     <- ชุดเดียว
    ├── reference/                    <- ไม่มีเลขเวอร์ชัน เก็บไว้
    └── ESP32-S3-FlowSim-Lab/         <- ไม่มีเลขเวอร์ชัน เก็บไว้
```

`docs/` `tools/` `hardware/` `README.md` `CLAUDE.md` เก็บไว้ **ตามสภาพ ณ commit ต้นทาง**
ห้ามเอาของปัจจุบันไปแปะย้อนหลัง เพราะคู่มือวันนี้อธิบายเฟิร์มแวร์คนละเวอร์ชันกับที่อยู่ในแบรนช์
จะกลายเป็นข้อมูลผิด แบรนช์เก่าบางตัวจึงไม่มี `hardware/` หรือ `CLAUDE.md` ซึ่งถูกต้องแล้ว

### ไฟล์ VERSION.md

ทุกแบรนช์ `release/*` ต้องมี `VERSION.md` ที่รากของแบรนช์ บอก

* คอลเลกชันและชนิดจอ เลขเวอร์ชัน โฟลเดอร์ซอร์ส commit ต้นทาง
* **ลำดับการพัฒนาทั้งคอลเลกชัน** พร้อมชี้ว่าแบรนช์นี้อยู่ตำแหน่งไหน ก่อนหน้าคืออะไร ถัดไปคืออะไร
* เวอร์ชันล่าสุดของสายอื่น ณ commit นั้น เพื่อบอกว่าใช้คู่กับอะไร
* สิ่งที่มีจริงในแบรนช์ และสิ่งที่ยังไม่มีเพราะยังไม่ได้สร้าง ณ ตอนนั้น

การตัดทำด้วย commit ใหม่ที่ **ต่อท้าย** commit ต้นทาง ไม่ใช่การเขียนประวัติทับ
ประวัติเดิมยังดูได้จาก `git log` และ push แบบ fast-forward ได้ตามปกติ

### ข้อห้าม

* **ห้ามย้ายแบรนช์ `release/<คอลเลกชัน>/v<เวอร์ชัน>` ไปยัง commit อื่นในสายงาน**
  เพราะจุดประสงค์คือตรึงภาพไว้ ถ้าแก้เวอร์ชันเดิมจนมีนัยสำคัญ
  ให้เลื่อนเลขเวอร์ชันแล้วสร้างแบรนช์ใหม่แทน
  ข้อยกเว้นเดียวคือ commit ต่อท้ายที่ปรับ **รูปทรงของแบรนช์เอง** เช่นตัดเฟิร์มแวร์
  เวอร์ชันอื่นออก หรือปรับ `VERSION.md` เพราะเป็น fast-forward ไม่ได้เขียนประวัติทับ
  และไม่ได้แตะซอร์สโค้ดของเวอร์ชันที่ตรึงไว้
* **ห้ามลบแบรนช์ `release/<คอลเลกชัน>/v<เวอร์ชัน>`** แม้เวอร์ชันนั้นจะเลิกใช้แล้ว
  (ไม่รวมแบรนช์ชื่อรูปแบบเก่า `release/<ส่วนประกอบ>-v<เวอร์ชัน>` ซึ่งเลิกใช้แล้ว
  และให้ลบทิ้งเมื่อทำได้ เพราะเนื้อหาถูกยกไปอยู่ในรูปแบบใหม่ครบแล้ว)
* **ห้าม merge แบรนช์ `release/*` เข้า `main` หรือเข้าแบรนช์งาน เด็ดขาด**
  แบรนช์ `release/*` มี commit ที่ **ลบ** โฟลเดอร์เฟิร์มแวร์เวอร์ชันอื่นออก
  ซึ่งถูกต้องสำหรับแบรนช์นั้น แต่ถ้า merge เข้า `main` การลบจะตามไปด้วย
  แล้ว `main` จะสูญเวอร์ชันไปจริง ๆ
  แบรนช์ `release/*` เป็นปลายทางอย่างเดียว ไหลออกจาก `main` เท่านั้น ไม่ไหลกลับ
  ห้ามเปิด PR จากแบรนช์ `release/*` ด้วย เพราะปุ่ม merge จะอยู่ตรงนั้นให้กดพลาด

## กฎการเลื่อนเลขเวอร์ชัน

**ห้ามแก้ทับเวอร์ชันเดิม** ทุกการปรับปรุงต้องสร้างโฟลเดอร์เวอร์ชันใหม่เสมอ
เพื่อให้เครื่องที่แฟลชเฟิร์มแวร์เดิมไปแล้วยังเทียบกับต้นฉบับได้

## โครงสร้างแพ็กเก็ต (Protocol v3)

`struct_message` และ `struct_host_sync` ต้องเหมือนกันทุกไบต์ในทุกเฟิร์มแวร์
ตรวจด้วย `bash tools/protocol-test/run.sh` ก่อน commit ทุกครั้งที่แตะโครงสร้างเหล่านี้

## ชุดตรวจก่อน commit

```bash
python3 tools/check-ino-types.py        # ลำดับการประกาศชนิดข้อมูลของ Arduino
bash tools/protocol-test/run.sh         # โครงสร้างแพ็กเก็ตตรงกันทุกรุ่น
bash tools/detector-test/run.sh         # อัลกอริทึมตรวจจับหยด
bash tools/host-rx-test/run.sh          # เส้นทางรับข้อมูลฝั่ง Host
bash tools/rate-test/run.sh             # สูตรคำนวณอัตราการไหล
bash tools/oled-check/run.sh            # คอมไพล์ Host รุ่นจอ OLED
bash tools/touch-preview/run.sh         # คอมไพล์และเรนเดอร์ Host รุ่นจอสัมผัส
bash hardware/verify.sh                 # ผังวงจรตรงกับหมายเลขขาในเฟิร์มแวร์
bash hardware/verify_pcb.sh             # DRC ของแผ่นวงจรพิมพ์
```

## คู่มือ

สร้างด้วย `bash tools/manual-build/run.sh` และ `run_web.sh`
ต้องมีฟอนต์ TH Sarabun ก่อน มิฉะนั้น PDF จะถูกแทนด้วยฟอนต์อื่นที่ผิดรูปแบบราชการ
สคริปต์จะเรียก `install_fonts.sh` ให้เองถ้ายังไม่มี

## สิ่งที่ยังไม่ได้ทำ

เฟิร์มแวร์และแผ่นวงจรพิมพ์ทั้งหมด **ยังไม่ได้ทดสอบบนฮาร์ดแวร์จริง**
ทุกอย่างตรวจด้วยเครื่องมือจำลองบนเครื่อง PC เท่านั้น ระบุไว้ใน README ของทุกรุ่น
