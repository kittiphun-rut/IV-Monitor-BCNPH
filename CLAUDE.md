# แนวปฏิบัติสำหรับโครงการ Smart IV Alert

## โครงไฟล์เฟิร์มแวร์

**หนึ่งสาย หนึ่งโฟลเดอร์ ชื่อคงที่ตลอดกาล** ห้ามสร้างโฟลเดอร์ใหม่ตอนขึ้นเวอร์ชัน

```
firmware/
├── Station/          เครื่องประจำเตียง สายหลัก
├── Station-Stable/   เครื่องประจำเตียง สายเสถียร (อัลกอริทึมตรวจจับเดิม)
├── Host-OLED/        เครื่องส่วนกลาง จอ SH1106 128×64
├── Host-TFT28/       เครื่องส่วนกลาง จอ ST7789V 2.8" 320×240 แนวนอน
├── Host-Touch24/     เครื่องส่วนกลาง จอสัมผัส ILI9341+XPT2046 2.4" 240×320
├── FlowSim-Lab/      เครื่องมือจำลอง ไม่มีเลขเวอร์ชัน
└── reference/        เอกสารอ้างอิง ไม่มีเลขเวอร์ชัน
```

ชื่อไฟล์ `.ino` ต้องตรงกับชื่อโฟลเดอร์เสมอ เพราะ Arduino IDE บังคับ
เช่น `firmware/Station/Station.ino`

**เลขเวอร์ชันอยู่ใน `#define APP_VERSION` ไม่ได้อยู่ในชื่อโฟลเดอร์**
ประวัติของเวอร์ชันเก่าเก็บไว้ใน git และในแบรนช์ `release/*` ซึ่งทำหน้าที่นี้
ได้ครบถ้วนกว่าการทิ้งโฟลเดอร์ค้างไว้ในต้นไม้ไฟล์ และไม่ทำให้สับสนว่าตัวไหนคือตัวจริง

## การออกเวอร์ชันและแบรนช์

**ทุกครั้งที่เลื่อน `APP_VERSION` ของสายใดสายหนึ่ง ต้องสร้างแบรนช์กำกับไว้เสมอ**

### รูปแบบชื่อแบรนช์

```
release/<ลำดับ ๒ หลัก>-s<เวอร์ชัน Station>-h<เวอร์ชัน Host OLED>[-ส่วนขยาย]
```

ส่วนขยายใส่เฉพาะสายที่ขยับในรุ่นนั้น

| ส่วนขยาย | ความหมาย | ตัวอย่าง |
|---|---|---|
| `-lts<เวอร์ชัน>` | สายเสถียรของ Station ขยับ | `release/13-s7.7.3-h4.7.5-lts7.5.4` |
| `-tft<เวอร์ชัน>` | Host จอ TFT 2.8" ขยับ | `release/02-s7.5.0-h4.7.0-tft4.8.0` |
| `-touch<เวอร์ชัน>` | Host จอสัมผัส 2.4" ขยับ | `release/14-s7.7.3-h4.7.5-touch4.9.2` |

ลำดับ ๒ หลักทำให้เรียงตามเวลาได้เองในหน้า Branches
ชื่อแบรนช์บอกทันทีว่า **จะแฟลชทั้งระบบต้องใช้เวอร์ชันอะไรบ้าง** โดยไม่ต้องเปิดดูข้างใน

### แบรนช์ต้องชี้ไปที่ commit ใด

ชี้ไปที่ **commit ที่เลื่อนเลขเวอร์ชันนั้น** ไม่ใช่ปลายแบรนช์งาน
แบรนช์เป็นหมุดเวลาล้วน ๆ ไม่ต้องมี commit ต่อท้าย ไม่ต้องตัดอะไรออก
เพราะโฟลเดอร์ชื่อคงที่อยู่แล้ว เปิดมาก็เห็นชุดที่ใช้คู่กันทันที

```bash
git branch release/15-s7.8.0-h4.7.5 <commit>
git push -u origin release/15-s7.8.0-h4.7.5
```

### สารบัญ

`VERSIONS.md` ที่รากรีโพคือสารบัญรวมทุกรุ่น ต้องเติมแถวใหม่ทุกครั้งที่ออกรุ่น
มีคอลัมน์ เวอร์ชันครบทั้ง ๕ สาย · สถานะคอมไพล์ · ลิงก์โหลด zip · ลิงก์คอมมิต ·
สาระสำคัญ · วันที่ เพื่อให้คนที่ไม่ใช้คำสั่ง git โหลดเฟิร์มแวร์ไปใช้ได้จากหน้าเดียว

### ข้อห้าม

* **ห้ามย้ายแบรนช์ `release/*` ที่สร้างไปแล้ว** และ **ห้าม push ทับ**
  เพราะจุดประสงค์คือตรึงภาพไว้ ถ้าแก้จนมีนัยสำคัญให้เลื่อนเลขเวอร์ชัน
  แล้วสร้างแบรนช์รุ่นถัดไปแทน
* **ห้ามลบแบรนช์ `release/*`** แม้รุ่นนั้นจะเลิกใช้แล้ว
* **ห้าม merge แบรนช์ `release/*` เข้า `main` หรือเข้าแบรนช์งาน เด็ดขาด**
  และห้ามเปิด PR จากแบรนช์ `release/*` เพราะปุ่ม merge จะอยู่ตรงนั้นให้กดพลาด
  แบรนช์ `release/*` เป็นปลายทางอย่างเดียว ไหลออกจาก `main` ไม่ไหลกลับ
  (เคยพลาดมาแล้ว PR #5 และ #6 ทำให้ `main` เสียเฟิร์มแวร์ไป ๔ เวอร์ชัน)
* **ห้ามดัดแปลงแบรนช์รุ่นเก่าให้เข้าโครงไฟล์ใหม่ย้อนหลัง**
  รุ่น `00`–`14` เกิดก่อนการจัดโครงใหม่ จึงยังเป็นโฟลเดอร์แยกตามเลขเวอร์ชัน
  ซึ่งถูกต้องแล้ว เพราะเป็นภาพจริงของรีโพ ณ ตอนนั้น

## โครงสร้างแพ็กเก็ต (Protocol v3)

`struct_message` และ `struct_host_sync` ต้องเหมือนกันทุกไบต์ในทุกเฟิร์มแวร์
ตรวจด้วย `bash tools/protocol-test/run.sh` ก่อน commit ทุกครั้งที่แตะโครงสร้างเหล่านี้

## ชุดตรวจก่อน commit

```bash
python3 tools/check-ino-types.py        # ลำดับการประกาศชนิดข้อมูลของ Arduino
bash tools/protocol-test/run.sh         # โครงสร้างแพ็กเก็ตตรงกันทุกรุ่น
bash tools/detector-test/run.sh         # อัลกอริทึมตรวจจับหยด
bash tools/host-rx-test/run.sh          # เส้นทางรับข้อมูลฝั่ง Host
bash tools/rate-test/run.sh             # สูตรคำนวณอัตราการไหล
bash tools/oled-check/run.sh            # คอมไพล์ Host รุ่นจอ OLED
bash tools/touch-preview/run.sh         # คอมไพล์และเรนเดอร์ Host รุ่นจอสัมผัส
bash hardware/verify.sh                 # ผังวงจรตรงกับหมายเลขขาในเฟิร์มแวร์
bash hardware/verify_pcb.sh             # DRC ของแผ่นวงจรพิมพ์
```

## คู่มือ

สร้างด้วย `bash tools/manual-build/run.sh` และ `run_web.sh`
ต้องมีฟอนต์ TH Sarabun ก่อน มิฉะนั้น PDF จะถูกแทนด้วยฟอนต์อื่นที่ผิดรูปแบบราชการ
สคริปต์จะเรียก `install_fonts.sh` ให้เองถ้ายังไม่มี

## สิ่งที่ยังไม่ได้ทำ

เฟิร์มแวร์และแผ่นวงจรพิมพ์ทั้งหมด **ยังไม่ได้ทดสอบบนฮาร์ดแวร์จริง**
ทุกอย่างตรวจด้วยเครื่องมือจำลองบนเครื่อง PC เท่านั้น ระบุไว้ใน README ของทุกรุ่น
