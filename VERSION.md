# Station (เครื่องประจำเตียง) v7.5.3

แบรนช์ `release/station/v7.5.3` — ภาพนิ่งของรีโพ ณ ตอนที่เวอร์ชันนี้เสร็จ

| หัวข้อ | รายละเอียด |
|---|---|
| คอลเลกชัน | `station` — เครื่องประจำเตียง (จอ TFT ในตัว) |
| เวอร์ชัน | 7.5.3 |
| โฟลเดอร์ซอร์ส | `firmware/ESP32-S3-Station-V_7_5_3/` |
| Commit ต้นทาง | `319fc4530a96` |
| งานที่ทำในเวอร์ชันนี้ | Host v4.7.3 + Station v7.7.2/v7.5.3: แก้อาการ Host ขึ้น OFFLINE เมื่อมีหลายเตียง |

## ลำดับการพัฒนาในคอลเลกชัน `station`

1. `release/station/v7.5.0` — fix(firmware): ย้ายการประกาศ enum/struct ขึ้นต้นไฟล์ ให้ Arduino IDE คอมไพล์ผ่าน
2. `release/station/v7.5.1` — station: แก้การวาดกราฟทำให้ไม่นับหยด + เรียนรู้หยดต่อเนื่อง + กันถุงแกว่ง (v7.5.1)
3. `release/station/v7.5.2` — Station v7.5.2: พอร์ตการหมุนจอ 180 องศาจาก v7.7.1 ลงสายเสถียร
4. `release/station/v7.5.3` **← แบรนช์นี้** — Host v4.7.3 + Station v7.7.2/v7.5.3: แก้อาการ Host ขึ้น OFFLINE เมื่อมีหลายเตียง
5. `release/station/v7.5.4` — แก้สูตรคำนวณอัตราการไหลและบั๊กฝั่ง Host (Station 7.7.3/7.5.4, Host 4.7.5)
6. `release/station/v7.6.0` — station: รองรับเซ็นเซอร์หยดแบบเอาต์พุตดิจิทัล + ผังขาใหม่ (v7.6.0)
7. `release/station/v7.7.0` — station: ไม่ต้องคาลิเบรตด้วยมืออีกต่อไป (v7.7.0)
8. `release/station/v7.7.1` — Station v7.7.1: หมุนจอ 180 องศาได้ที่หน้าเครื่อง
9. `release/station/v7.7.2` — Host v4.7.3 + Station v7.7.2/v7.5.3: แก้อาการ Host ขึ้น OFFLINE เมื่อมีหลายเตียง
10. `release/station/v7.7.3` — แก้สูตรคำนวณอัตราการไหลและบั๊กฝั่ง Host (Station 7.7.3/7.5.4, Host 4.7.5)

ก่อนหน้า: `release/station/v7.5.2`

ถัดไป: `release/station/v7.5.4`

## ใช้คู่กับเวอร์ชันใด

เวอร์ชันล่าสุดของสายอื่น ณ commit ต้นทาง คือ

* Host จอ OLED → `release/oled/v4.7.3`
* Host จอ TFT 2.8" → `release/tft28/v4.8.0`
* Host จอสัมผัส 2.4" → `release/touch24/v4.9.0`

ซอร์สของสายอื่นไม่ได้อยู่ในแบรนช์นี้ ให้เปิดจากแบรนช์ของมันเอง

## สิ่งที่มีจริงในแบรนช์นี้

* `firmware/ESP32-S3-Station-V_7_5_3/` — เฟิร์มแวร์ของเวอร์ชันนี้ ชุดเดียว
* `tools/`
* `docs/`
* `README.md`
* `firmware/reference/`, `firmware/ESP32-S3-FlowSim-Lab/` — ไม่ได้แยกตามเวอร์ชัน จึงเก็บไว้

**ไม่มีในแบรนช์นี้:** `hardware`, `CLAUDE.md`
เพราะยังไม่ได้สร้างขึ้น ณ ตอนที่เวอร์ชันนี้เสร็จ ไม่ได้ถูกลบ ดูของล่าสุดได้ที่แบรนช์ `main`

เอาออกจากแบรนช์นี้ 13 โฟลเดอร์ คือเฟิร์มแวร์เวอร์ชันอื่นทั้งหมด
ทุกอย่างยังดูได้จาก `git log` และจากแบรนช์ของมันเอง

## ข้อจำกัด

เฟิร์มแวร์และแผ่นวงจรพิมพ์ของโครงการนี้ **ยังไม่ได้ทดสอบบนฮาร์ดแวร์จริง**
ตรวจด้วยเครื่องมือจำลองบนเครื่อง PC เท่านั้น

ชุดตรวจใน `tools/` บางตัวตรึงชื่อโฟลเดอร์เวอร์ชันอื่นไว้ในสคริปต์
ตัวเหล่านั้นจะรันไม่ผ่านบนแบรนช์นี้ ให้รันบนแบรนช์งานหลักที่มีเฟิร์มแวร์ครบทุกเวอร์ชันแทน
